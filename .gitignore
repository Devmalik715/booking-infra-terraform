# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
# tfvars are committed here on purpose - they hold sizing, not secrets - but
# never commit an override file with credentials in it.
*.auto.tfvars
override.tf
override.tf.json

# Local environment
.env

# Database dumps
backups/
*.dump
*.sql.gz

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
